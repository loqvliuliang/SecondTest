<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.persistence.RealmMapper">

   <select id="findAll" parameterType="com.example.demo.domain.Realm">
       SELECT * from alt_realm
   </select>

</mapper>